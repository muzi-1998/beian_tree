# §1.1 Decomposition — 三处软肋的配置/代码补强 diff

针对验证暴露的三处问题给出**可直接 patch** 的改动,均锚定在仓库现有真实代码行上：

| 补法 | 解决的软肋 | 触及文件 |
|---|---|---|
| 1 分解充分性 | 主周期峰<2 仅 9/33;ORP/QR 残留峰 11~16 | `configs/deperiodise.yaml`、`src/baseline/deperiodise.py`、`run_pipeline.py`(W2) |
| 2 白化 ARCH + 接受门兜底 | min 级创新 ARCH-LM 仍显著;19/33 gate fail 仍产出创新 | `configs/whiten.yaml`、`src/whiten/offline_identify.py`、`src/whiten/online_whitener.py`、`src/whiten/diagnostics.py`、`run_pipeline.py`(process) |
| 3 验证两实验 | 消融 AUC 饱和 1.0;出水差异化无对比 | `validate.py` |

> 应用：`git apply --recount fix.patch` 或 `patch -p1 --fuzz=3 < fix.patch`;行号为指示性,hunk 已锚在唯一上下文行上。函数级替换直接整段替换即可。

---

## 补法 1：分解充分性 —— 抬高 ORP/QR 谐波阶数 + 迭代 STL + 充分性闸门

### 1a. `configs/deperiodise.yaml`

```diff
   aerobic_do:        # DO_x_1..3 — aeration day cycle + 12h harmonic (strong)
     track: min
     candidate_periods: [1440, 720]
     harmonic_order_min: 1
     harmonic_order_max: 6
     harmonic_order_init: 3
     loess_trend_bandwidth_h: 24
+    stl_refine_iters: 2          # 谐波后再迭代 STL,吃掉非正弦残留
   postanoxic_do:     # DO_x_4 — near-zero floor; harmonics weak, censoring path
     track: min
     candidate_periods: [1440]
     harmonic_order_min: 0
     harmonic_order_max: 1
     harmonic_order_init: 0
     loess_trend_bandwidth_h: 24
     floor_censoring: true        # treat near-floor as left-censored
   anoxic_orp:        # ORP_x_1..3 — weak day cycle, trend/regime dominated
     track: min
-    candidate_periods: [1440]
+    candidate_periods: [1440, 720]   # 补 12h 谐波(ORP 日周期非正弦)
     harmonic_order_min: 1
-    harmonic_order_max: 3
+    harmonic_order_max: 5            # ORP_1_3 残留峰 11.2 -> 放宽阶数
     harmonic_order_init: 1
     loess_trend_bandwidth_h: 24
+    stl_refine_iters: 3             # 主要靠迭代 STL 压非正弦周期
   flow:              # QR/QIR — exogenous driver, day + week cycle (Class A)
     track: min
     candidate_periods: [1440, 10080]
     harmonic_order_min: 1
-    harmonic_order_max: 4
+    harmonic_order_max: 6            # QR_2 残留峰 15.7 -> 放宽阶数
     harmonic_order_init: 2
     loess_trend_bandwidth_h: 168
+    stl_refine_iters: 3
```

并在全局 STL 段补两个默认键：

```diff
 # STL
 stl_period_min: 1440
 stl_period_hour: 24
 stl_seasonal_smoother: 7
+stl_refine_iters_default: 1              # 未显式声明的组默认迭代 1 次
+stl_max_refine_iters: 4                  # 安全上限,防过拟合
 residual_spectrum_peak_ratio_max: 2.0    # peak/baseline < 2 => period removed
```

### 1b. `src/baseline/deperiodise.py` —— 新增一个顶层辅助函数(追加,不动现有主体)

复用现有的 `_causal_stl_seasonal`,严格因果。放在 `decompose_channel` 之后即可：

```python
def extra_stl_pass(residual: pd.Series, period: int, n_cycles: int = 14,
                   min_cycles: int = 3) -> pd.Series:
    """对已分解的残差再做一次因果 STL 精修(plan §3.4 互补建模).

    谐波 + 首次 STL 之后仍残留的非正弦周期(ORP/QR 的方波/事件型日周期),
    用同相位、仅取过去 n_cycles 个周期均值的方式再减一层。严格因果。
    """
    e = residual.values.astype(float)
    seas = _causal_stl_seasonal(e, period, n_cycles=n_cycles, min_cycles=min_cycles)
    return pd.Series(e - seas, index=residual.index, name=residual.name)
```

### 1c. `run_pipeline.py` —— W2 中把"分解充分性"做成闸门 + 迭代重剥

锚点是 W2 里计算 `pr` 与写 `suff_rows` 的两行：

```diff
         pr = deperiodise.residual_spectrum_peak_ratio(dec["residual"],
                                                       gcfg["candidate_periods"], dt_native)
-        suff_rows.append(dict(channel=c, track=track, **pr))
+        # ── Fix 1: 主周期峰 >= 闸门则迭代 STL 重剥,并落 decomp_pass 标志 ──────
+        gate = gcfg.get("decomp_peak_gate",
+                        cfg["deperiodise"]["residual_spectrum_peak_ratio_max"])
+        max_it = cfg["deperiodise"].get("stl_max_refine_iters", 4)
+        n_it = min(gcfg.get("stl_refine_iters",
+                            cfg["deperiodise"].get("stl_refine_iters_default", 1)), max_it)
+        prim_key = "P1440" if track == "min" else "P24"
+        prim_period = 1440 if track == "min" else 24
+        it = 0
+        while it < n_it and (pr.get(prim_key) or 0) >= gate:
+            dec["residual"] = deperiodise.extra_stl_pass(dec["residual"], prim_period)
+            pr = deperiodise.residual_spectrum_peak_ratio(
+                dec["residual"], gcfg["candidate_periods"], dt_native)
+            it += 1
+        decomp_pass = bool((pr.get(prim_key) or 0) < gate)
+        suff_rows.append(dict(channel=c, track=track,
+                              decomp_pass=decomp_pass, stl_iters=it, **pr))
         decomp_store[c] = dec
```

**预期效果**：`decomposition_sufficiency.csv` 新增 `decomp_pass`/`stl_iters` 两列;ORP/QR 的主周期峰比从 11~16 迭代降到接近或低于 2;验证报告"主周期峰<2 的通道数"从 9/33 显著上升,且未过的通道被**显式标注**(而非一句"交给白化")。

---

## 补法 2：白化 ARCH 升阶 + 接受门兜底

### 2a. `configs/whiten.yaml`

```diff
 use_garch: true
 garch_default: {p: 1, q: 1}
+garch_escalate: true                 # ARCH-LM 仍显著则升 GARCH 阶
+garch_grid: [[1, 1], [1, 2], [2, 1]] # (ARCH lags, GARCH lags) 逐级升
+egarch_fallback: false               # 可选:在线递归仅支持 GARCH,EGARCH 仅离线诊断
```

并在文件末尾(`acceptance:` 块之后)补"门失败兜底"：

```diff
 acceptance:
   require_stationary_invertible: true
   require_ljungbox_pass: true
   max_sigma2_ratio: 10.0
+
+# gate-fail 兜底 (plan §6.4 (六) 优雅降级)
+fallback:
+  on_gate_fail: robust_z             # robust_z | last_good | none
+  mad_constant: 1.4826
```

### 2b. `src/whiten/offline_identify.py` —— 用升阶版整体替换 `_fit_garch`

```python
def _fit_garch(innov: np.ndarray, grid=((1, 1), (1, 2), (2, 1)),
               escalate: bool = True, egarch_fallback: bool = False,
               arch_lags: int = 12, alpha: float = 0.05):
    """在创新上拟合 GARCH,逐级升阶直到 ARCH-LM 不再显著(plan §4.4).

    依次尝试 grid 中各 (ARCH, GARCH) 阶;若标准化残差 ARCH-LM 仍显著且 escalate,
    升到下一更大阶;最后(可选)回退 EGARCH(1,1)。返回含条件方差递归参数的 dict
    (alpha/beta 为按滞后排列的列表,GARCH(1,1) 即长度 1),或 None。
    """
    try:
        from arch import arch_model
    except Exception:
        return None
    x = np.asarray(innov, dtype=float)
    x = x[np.isfinite(x)]
    if len(x) < 200:
        return None
    best = None
    for (gp, gq) in grid:                       # gp = ARCH lags, gq = GARCH lags
        try:
            with warnings.catch_warnings():
                warnings.simplefilter("ignore")
                r = arch_model(x, mean="Zero", vol="GARCH", p=gp, q=gq,
                               rescale=False).fit(disp="off")
        except Exception:
            continue
        std = np.asarray(r.std_resid, dtype=float)
        het_p = arch_lm(std[np.isfinite(std)], lags=arch_lags).get("arch_p", 1.0)
        pr = r.params
        cand = dict(kind="garch", p=gp, q=gq,
                    omega=float(pr.get("omega", 0.0)),
                    alpha=[float(pr.get(f"alpha[{i}]", 0.0)) for i in range(1, gp + 1)],
                    beta=[float(pr.get(f"beta[{i}]", 0.0)) for i in range(1, gq + 1)],
                    arch_p_after=float(het_p))
        if best is None or het_p > best["arch_p_after"]:
            best = cand
        if het_p > alpha:                       # ARCH 已清除 -> 接受此阶
            return cand
        if not escalate:
            break
    if egarch_fallback:                          # 离线诊断用;在线递归暂不支持
        try:
            with warnings.catch_warnings():
                warnings.simplefilter("ignore")
                r = arch_model(x, mean="Zero", vol="EGARCH", p=1, o=1, q=1,
                               rescale=False).fit(disp="off")
            pr = r.params
            return dict(kind="egarch", p=1, q=1,
                        omega=float(pr.get("omega", 0.0)),
                        alpha=[float(pr.get("alpha[1]", 0.0))],
                        beta=[float(pr.get("beta[1]", 0.0))],
                        gamma=float(pr.get("gamma[1]", 0.0)))
        except Exception:
            pass
    return best
```

同文件 `identify()` 内构造 `warmup_state` 处(现为 `eps_hist/eta_hist/sigma2`),改为给 GARCH 留足创新与方差历史：

```diff
-    eps_hist = list(yc[-p:][::-1]) if p > 0 else []        # newest first
-    eta_hist = list(innov[-q:][::-1]) if q > 0 else []
-    sigma2 = float(np.var(innov)) if len(innov) else 1.0
-    warmup_state = dict(eps=eps_hist, eta=eta_hist, sigma2=sigma2)
+    na = len(garch.get("alpha", [])) if garch else 0
+    nb = len(garch.get("beta", [])) if garch else 0
+    keep_eta = max(q, na)                                  # GARCH 需过去创新,即使 q=0
+    eps_hist = list(yc[-p:][::-1]) if p > 0 else []        # newest first
+    eta_hist = list(innov[-keep_eta:][::-1]) if keep_eta > 0 else []
+    sigma2 = float(np.var(innov)) if len(innov) else 1.0
+    warmup_state = dict(eps=eps_hist, eta=eta_hist, sigma2=sigma2,
+                        sig2_hist=[sigma2] * max(nb, 1))
```

### 2c. `src/whiten/online_whitener.py` —— GARCH 递归推广到 (p,q),与升阶后的参数对齐

`__init__` 加一个方差历史缓冲：

```diff
         self._eta = np.zeros(0)     # innovation history, newest first
         self._sig2 = 1.0
+        self._s2 = np.zeros(0)      # past conditional variances, newest first
         self._mu = 0.0
```

`_hot_swap` 载入方差热态：

```diff
         self._eps = np.array(ws.get("eps", []), dtype=float)
         self._eta = np.array(ws.get("eta", []), dtype=float)
         self._sig2 = float(ws.get("sigma2", 1.0)) or 1.0
+        self._s2 = np.array(ws.get("sig2_hist", []), dtype=float)
```

`step()` 中 GARCH 计算块(标量 → 向量化的 (p,q)):

```diff
         if m.garch:
             g = m.garch
-            eta_prev = self._eta[0] if len(self._eta) else 0.0
-            sig2 = g["omega"] + g["alpha"] * eta_prev ** 2 + g["beta"] * self._sig2
-            sig2 = max(sig2, 1e-12)
+            a = np.atleast_1d(np.asarray(g.get("alpha", 0.0), float))   # eta 滞后系数
+            b = np.atleast_1d(np.asarray(g.get("beta", 0.0), float))    # sig2 滞后系数
+            e2 = (self._eta[:len(a)] ** 2) if len(self._eta) else np.zeros(0)
+            e2 = np.pad(e2, (0, len(a) - len(e2)))
+            s2 = self._s2[:len(b)] if len(self._s2) else np.zeros(0)
+            s2 = np.pad(s2, (0, len(b) - len(s2)))
+            sig2 = max(float(g["omega"]) + float(a @ e2) + float(b @ s2), 1e-12)
         else:
             sig2 = self._sig2
```

`step()` 末尾推历史(保证 GARCH 在 q=0 时也维护创新历史,并新增方差历史):

```diff
         if p:
             self._eps = np.r_[y, self._eps][:p]
-        if q:
-            self._eta = np.r_[eta, self._eta][:q]
+        na = len(np.atleast_1d(np.asarray(m.garch.get("alpha", 0.0), float))) if m.garch else 0
+        keep_eta = max(q, na)
+        if keep_eta:
+            self._eta = np.r_[eta, self._eta][:keep_eta]
+        if m.garch:
+            nb = len(np.atleast_1d(np.asarray(m.garch.get("beta", 0.0), float)))
+            self._s2 = np.r_[sig2, self._s2][:max(nb, 1)]
         self._sig2 = sig2
         return eta / np.sqrt(sig2)
```

> 注：`whiten_series`(批处理 `lfilter` 版)的 GARCH 同样要按 (p,q) 递归;若它内部也写死 (1,1),用同一向量化式替换即可。

### 2d. `src/whiten/diagnostics.py` —— 新增兜底白化 `robust_z`

```python
def robust_z(x: pd.Series, mad_c: float = 1.4826) -> pd.Series:
    """接受门失败时的兜底"白化"(plan §6.4 优雅降级).

    用 MAD 标准化残差。它不是真白化(仍含自相关),但有界、稳健,确保不合格模型
    永不被静默发布——失败通道得到一个降级但可用的分数,而非伪精确的创新。
    """
    v = x.astype(float)
    med = v.median()
    mad = mad_c * (v - med).abs().median() + 1e-9
    return (v - med) / mad
```

### 2e. `run_pipeline.py` —— `process()` 中接受门失败即走兜底

锚点是 `acceptance_gate(...)` 调用之后、warm-restart 注释之前：

```diff
         passed, reasons = ag.acceptance_gate(model, float(np.nanvar(resid)),
                                               innov.dropna().values, wcfg,
                                               diag=model.diagnostics)
+        # ── Fix 2: 接受门失败 -> 兜底,而不是照样发布失败模型的创新 ──────────
+        fb = wcfg.get("fallback", {}).get("on_gate_fail", "none")
+        used_fallback = False
+        if not passed and fb == "robust_z":
+            z = dg.robust_z(resid)            # MAD 标准化残差兜底
+            innov = z.copy()
+            used_fallback = True
         # warm-restart state refresh on the last warmup_hours
```

并在随后的 `arma_rows.append(dict(...))` 里加一列 `fallback`：

```diff
         arma_rows.append(dict(channel=c, track=track,
                               p=model.p, q=model.q,
                               garch=model.garch is not None,
                               aic=model.diagnostics.get("aic"),
                               bic=model.diagnostics.get("bic"),
-                              accepted=passed, gate=";".join(reasons)))
+                              accepted=passed, fallback=used_fallback,
+                              gate=";".join(reasons)))
```

**预期效果**：升阶 GARCH 后 min 级创新 ARCH-LM `het` 应有相当比例由 True 转 False;`arma_garch_order_table.csv` 新增 `fallback` 列;19 个 gate-fail 通道不再输出"伪精确"创新,而是带 `fallback=True` 的 robust-z 兜底分,与"接受门"语义自洽。

---

## 补法 3：消融分级幅度 + 出水差异化重设计

### 3a. `validate.py` —— 用分级幅度整体替换 `exp_ablation`

```python
def exp_ablation(df_min, resid_min, cfg, channel="DO_1_3",
                 n_faults=200, rng_seed=0,
                 amp_mults=(1.0, 1.5, 2.0, 3.0, 4.0, 6.0)):
    """注入分级幅度的同一可加尖峰,报 AUC 随幅度的曲线(替代单点 1.0).

    幅度以残差 MAD 的倍数表达;对 raw / residual / 一步创新 分别 robust-z 评分。
    返回每个幅度一行,便于画 AUC-幅度曲线,避免大幅故障导致 AUC 饱和。
    """
    rng = np.random.RandomState(rng_seed)
    resid = resid_min[channel]
    grid = cfg["whiten"]["arma_grid"]["min"]
    model = oid.identify(resid.iloc[:cfg["whiten"]["cold_start_reference_days"] * 1440],
                         None, grid, cfg["whiten"]["use_garch"], version=f"{channel}_abl")
    eta = ow.whiten_series(resid, model)["innovation"]

    raw = df_min[channel]
    idx = raw.dropna().index.intersection(resid.dropna().index).intersection(eta.dropna().index)
    raw, resid, eta = raw.loc[idx], resid.loc[idx], eta.loc[idx]
    n = len(idx)
    scale = 1.4826 * np.median(np.abs(resid.values - np.median(resid.values)))

    def rz(x):
        c = np.median(x); s = 1.4826 * np.median(np.abs(x - c)) + 1e-9
        return np.abs((x - c) / s)

    rows = []
    for mult in amp_mults:
        labels = np.zeros(n, dtype=int)
        pos = rng.choice(np.arange(60, n - 60), size=min(n_faults, n // 50), replace=False)
        raw_s, resid_s, eta_s = raw.values.copy(), resid.values.copy(), eta.values.copy()
        for p in pos:
            A = mult * scale * rng.choice([-1, 1])      # 固定幅度倍数
            raw_s[p] += A; resid_s[p] += A; eta_s[p] += A
            labels[p] = 1
        rows.append(dict(channel=channel, amp_mult=mult, n_faults=int(labels.sum()),
                         auc_raw=round(_auc(rz(raw_s), labels), 4),
                         auc_residual=round(_auc(rz(resid_s), labels), 4),
                         auc_innovation=round(_auc(rz(eta_s), labels), 4)))
    return pd.DataFrame(rows)
```

> `validate.py` 的 `main()` 里现在是 `abls.append(exp_ablation(...))` 后 `abl_df = pd.DataFrame(abls)`。因为新函数已返回 DataFrame,改为：
> ```diff
> -    abls = []
> -    for ch in ["DO_1_3", "ORP_2_1"]:
> -        if ch in resid_min:
> -            abls.append(exp_ablation(df_min, resid_min, cfg, ch))
> -    abl_df = pd.DataFrame(abls)
> +    abl_df = pd.concat([exp_ablation(df_min, resid_min, cfg, ch)
> +                        for ch in ["DO_1_3", "ORP_2_1"] if ch in resid_min],
> +                       ignore_index=True)
> ```

### 3b. `validate.py` —— 出水差异化对照重设计(替换 eff_COD 那一段)

原对照"出水强行套 24h 6 阶谐波"在出水无 24h 峰时形成不了压力。改为**正确(STL/低阶, 168h 带宽)vs 错误(min 风格:24h 短带宽 LOESS + 高阶日谐波)**,并改用**残差方差 + 趋势过拟合比**而非 24h 峰比来度量：

```diff
     if "eff_COD" in eff_f:
         s2 = eff_f["eff_COD"]
         proper2 = deperiodise.decompose_channel(s2, groups["effluent"], 1.0, fit_days)["residual"]
-        wrong_cfg = dict(groups["effluent"])            # dimensionally valid (hours)
-        wrong_cfg.update(candidate_periods=[24, 12], harmonic_order_min=6,
-                         harmonic_order_max=6, harmonic_order_init=6)
-        wrong2 = deperiodise.decompose_channel(s2, wrong_cfg, 1.0, fit_days)["residual"]
-        rows.append(dict(channel="eff_COD", proper="effluent(STL,order<=2)",
-                         wrong="forced 24h order-6 harmonics",
-                         proper_peakratio_24h=deperiodise.residual_spectrum_peak_ratio(
-                             proper2, [24], 1.0).get("P24"),
-                         wrong_peakratio_24h=deperiodise.residual_spectrum_peak_ratio(
-                             wrong2, [24], 1.0).get("P24"),
-                         proper_resid_std=round(float(proper2.std()), 4),
-                         wrong_resid_std=round(float(wrong2.std()), 4)))
+        dec_p = deperiodise.decompose_channel(s2, groups["effluent"], 1.0, fit_days)
+        proper2 = dec_p["residual"]
+        # 错误策略 = min 风格套到出水:24h 短带宽 LOESS(过紧) + 高阶日谐波(过拟合)
+        wrong_cfg = dict(groups["effluent"])
+        wrong_cfg.update(candidate_periods=[24, 12], harmonic_order_min=4,
+                         harmonic_order_max=4, harmonic_order_init=4,
+                         loess_trend_bandwidth_h=24)        # 出水本应 168h
+        dec_w = deperiodise.decompose_channel(s2, wrong_cfg, 1.0, fit_days)
+        wrong2 = dec_w["residual"]
+        # 度量改为:残差方差(过拟合会吃掉真信号->残差异常小或被搬走) +
+        # 被移除量方差比(错误策略把慢变信号当周期/趋势搬走 -> 移除量方差膨胀)
+        rem_p = (s2.reindex(proper2.index) - proper2).var()
+        rem_w = (s2.reindex(wrong2.index) - wrong2).var()
+        rows.append(dict(channel="eff_COD", proper="effluent(STL,168h,order<=2)",
+                         wrong="min-style(24h LOESS + order-4 daily harmonics)",
+                         proper_resid_std=round(float(proper2.std()), 4),
+                         wrong_resid_std=round(float(wrong2.std()), 4),
+                         removed_var_proper=round(float(rem_p), 4),
+                         removed_var_wrong=round(float(rem_w), 4),
+                         overfit_ratio=round(float(rem_w / (rem_p + 1e-9)), 3)))
     return pd.DataFrame(rows)
```

### 3c. `validate.py` —— 报告叙述微调(`main()` 里第 4、5 节文字)

```diff
-    L("- 同一注入故障下，AUC: 原始序列 < 去周期残差 < 白化创新，"
-      "证明去周期+白化提升了故障可分性 (支撑大纲 1.4.2 变体 A)。\n")
+    L("- AUC 随注入幅度上升;在各幅度下均有 原始 < 去周期残差 < 白化创新，"
+      "证明去周期+白化提升故障可分性,且小幅故障下增益最明显 (支撑大纲 1.4.2 变体 A)。\n")
```

```diff
-    L("- 用错误策略 (出水低阶套到 min DO / min 高阶套到出水) 后，目标周期残差峰比上升、"
-      "残差方差变化，证明分钟级与小时级必须差异化分解。\n")
+    L("- min 端:错误策略使目标周期残差峰比从 ~7 飙到 ~91;出水端:错误策略(24h 短带宽+高阶日谐波)"
+      "把慢变信号当周期搬走,overfit_ratio>>1,证明分钟级与小时级必须差异化分解。\n")
```

**预期效果**：`val_ablation_auc.csv` 变为多行(每通道×多幅度),可画 AUC-幅度曲线,不再是单点 1.0;`val_differentiation_necessity.csv` 出水行的 `overfit_ratio` 应明显 >1,把原来 0.909 vs 0.908 的"无对比"换成有压力的对照。

---

## 应用与回归

```bash
cd "Project 1 Data Quality Assessment/1.1 Decomposition"
git apply --recount fix.patch        # 或逐文件手改 / patch -p1 --fuzz=3
python run_pipeline.py --quick       # 冒烟:先确认无报错、新列出现
python run_pipeline.py               # 全量
python validate.py                   # 重生成验证报告
```

回归核对三张表/报告：
- `decomposition_sufficiency.csv` 出现 `decomp_pass`、`stl_iters`;"主周期峰<2 通道数" 上升。
- `arma_garch_order_table.csv` 出现 `fallback`;创新 ARCH `het=True` 比例下降。
- `val_ablation_auc.csv` 多幅度多行;`val_differentiation_necessity.csv` 出水 `overfit_ratio>>1`。

## 一处需后续跟进(非本次必改)

`garch_grid`/`egarch_fallback` 目前由 `_fit_garch` 内置默认驱动;若要完全数据驱动,需把 `whiten.yaml` 的 garch 配置经 `identify()` 透传给 `_fit_garch`(给 `identify` 加一个 `garch_cfg=None` 关键字参数并在 `run_pipeline.process` 传 `garch_cfg=wcfg`)。EGARCH 改的是对数方差递归,在线 `OnlineWhitener` 暂未支持,故默认 `egarch_fallback: false`,仅在离线诊断中可用;若要在线 EGARCH,需在 `step()` 增加一支对数方差递归。
