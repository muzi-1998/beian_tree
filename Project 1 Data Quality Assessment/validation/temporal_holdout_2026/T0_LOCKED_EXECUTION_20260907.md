# T0新期自然数据评分锁定合同

本合同在2026-04-14至2026-07-30的DO/ORP分布和评分揭示前固定；准确UTC时间和代码、配置、模型、输入指纹由 `T0_opening_lock.json` 记录。属于采集后的同厂时间验证，不是前瞻性采集注册。

## 本次执行范围

- 108天原始分钟数据接入18列；14支DO/ORP评分，QR/QIR仅作既有协变量。
- T0自然评分、缺证据原因、低尾负担、DQR core12/Full/available与信息时钟。未复核报警不称真故障或误报。
- 不开展T1成熟化选模，不用新期数据拟合D5支持，不改变门槛、peer、映射和权重。受控注入、非劣验证及全部研究计划的统计验证仍为后续独立工作；自然评分不能替代它们。

## 冻结与续接

1. 保留原历史分钟网格直至2026-04-13 23:59，包括原末日空值；不以新导出的末日尾部回填历史。新期从次日00:00接入。
2. 分钟预处理只允许已验证的最长3分钟补齐，发布延迟3分钟；D2/D3/D5直接使用未填补原值。末端没有未来三分钟时，D1/D4最后未闭合小时不发布，名义网格保留缺证据。
3. 1.1冻结阶数、周期与白化参数，继承每14天仅由过去30天更新谐波系数的因果政策。不是所有系数永久不变，也不进行新期结构搜索。
4. D1恢复状态从历史证据日志续接；新期Spike/Step/Drift/Freeze/Regime均真实计算，不延长旧分数或旧残差。固定PLS和regime模型。已确认的缺证据不能通过历史前向填充成为可发布分数。
5. D2固定映射与参考，保留Strict/Sensitive；原始重复/乱序/真实不规则采样在对齐导出中不可观测，条件归一化，不计满分。长缺口与P95只能在当时可知范围内计算。
6. D3冻结分钟阈值与温度系数，2小时窗口结束才可用。DQR使用最新已经结束的2小时安全评估，明确其回看区间，不回填过去两个小时，不将诊断警告解释成数据故障。
7. D4使用真实新残差、原24小时common-support、冻结公共映射；变点仅使用当时闭合的辅助窗口和已知候选，禁止未来候选改写过去。D2仅做准入，D1只提供冻结工况标签，数值始终源自D4_raw。
8. D5冻结2026-01-27 04:30参考边界、完整ECDF、模板、hysteresis和56条支持记录；全量历史日志重放状态。不重新拟合，不升级L1。缺少当前观测或必要工况特征时正式接口NA。为避免BLAS批长度舍入被ECDF放大，使用确定的逐行求和；历史数值敏感性单独输出，不声称与旧pickle逐字节相同。
9. DQR决策时刻为小时标签+1小时3分钟。D1/D4最多1小时有效；D2闭合小时最多1小时；D5最近闭合10分钟快照所属小时最多1小时；D3闭合窗口最多2小时。较新的NA不会退回搜索更早的非NA分数。

## 解释与统计边界

固定core12为纵向共同维度；Full为完整证据子集，available必须附维度掩码。D3不进入质量均值，D4仅进入pair，证据强度不乘分数。A–E关闭。保留所有低分和缺证据，不根据新结果修改阈值。

日/月指标先报告明确分母的描述性结果；同期通道不是独立重复。需要区间时使用同步全厂7日块、2000次、种子20260907，2/14日敏感性，至少6个独立时间块才给区间；此门槛仅为本研究精度保护，不是统计定理。事件在真实时间轴提取，不能拼接不相邻块制造长事件。本次自然评分不宣称已完成区间覆盖率模拟或受控挑战验证。

## 可复现性

每次打开新原始数据前重新核对锁定内容。任何科学代码改变均停止并登记修订，不能静默覆盖开盲锁。重启策略是完整保留历史输入和状态日志后重放，不声称已经实现低内存实时服务。正式旧项目输出不改写，所有新结果位于本验证模块。
